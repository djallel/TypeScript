import "jQuery";
//pour prendre en compte le $ de la ligne 3,il faut runner la commande suivante pour installer Jquery npm install --save-dev @types/jquery
$("#app").css({"background-color": "green"});