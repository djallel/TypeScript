let myName: string = "Max";
let myAge: number = 27;
myName = 30
let anything;
anything = 12;

// myName = 30; ceci est une erreur car myName est un string
//important, pour myName = 30, même si c'est une erreur,
// il va quand même généré le code dans la app.javascript (app.js)